class PaymentConfig {
  /// Toggle this to `true` to show cash payment options across invoice screens.
  /// Set to `false` to hide all customer-facing cash payment UI (recommended
  /// when you don't want customers to use cash flow).
  static const bool allowCashPayments = false;
}
